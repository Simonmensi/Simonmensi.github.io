/**
 * @file guidelines/page.tsx
 * @description BIM Guidelines page stub — full content added in Phase 3.
 */

/**
 * Guidelines page — placeholder until Phase 3 BIM compliance content.
 *
 * @returns A minimal placeholder page element.
 */
export default function GuidelinesPage() {
  return (
    <main className="flex min-h-[80vh] flex-col items-center justify-center px-6 text-center">
      <h1 className="text-3xl font-bold text-blue-900">Guidelines</h1>
      <p className="mt-4 text-blue-900/60">BIM compliance content — coming soon in Phase 3.</p>
    </main>
  );
}
